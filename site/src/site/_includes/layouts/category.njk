---
layout: layouts/base.njk
templateClass: tmpl-pro
renderData:
  title: "{{category.content.main.name}}"
---


<div class='f jcb aist category__header rel bb'>
  <div class='container--xl mxa f jcb aist x'>
    <div class='category__header-browse rel z1 al bcg cw f jcs aic'>
      <span class='s18'>Browse by category:</span>
    </div>
    <div class='category__header-links z1 arrow-helper x al rel px1'>
      {# <span class='block s28'>{{category.content.main.name}}</span> #}
      <select name="navigation" id="navigationalDrop" data-component='easy' data-placeholder="{{category.content.main.name}}">
        <option data-placeholder='true'>{{category.content.main.name}}</option>
        {% for parent in parents %}
          <option class='dropdown--hidden' value='{{parent.parent}}' disabled>{{parent.parent}}</option>
          {% for listing in parent.listings %}
            <option class='dropdown-filters' value='/category/{{listing.content.main.slug.current}}'>
              <span>{{listing.content.main.name}}</span>
            </option>
          {% endfor %}
          {% if loop.last != true %}
            <option class='hidden'></option>
          {% endif %}
        {% endfor %}
      </select>
    </div>
  </div>
</div>

<div class='f category__content fw jcb ais container--xl mxa'> 
  {% include 'filters.njk' %}
  <div class='mt1 category__list js-category-list x p1'>
    {% for listing in category.content.listings %}
      {% include 'listing-card.njk' %}
    {% else %}
      <div class='rel category__empty'>
        <div class='bcw category__empty-text p1 ba mxa'>
          <p class='bold'>Oops! We're getting there.</p>
          <p>We don't have anything that fits those spects. We're always working on finding more, though.</p>
          <a href='' class='s16 caps cg'>Submit a Listing</a>
        </div>
        <img class='x category__empty-image mxa' src='/images/bottle-straw.webp' alt='trashcan newsletter' />
      </div>
    {% endfor %}
    <div class='rel category__empty js-empty hidden'>
      <div class='bcw category__empty-text p1 ba mxa'>
        <p class='bold'>Oops! We're getting there.</p>
        <p>We don't have anything that fits those spects. We're always working on finding more, though.</p>
        <a href='' class='s16 caps cg'>Submit a Listing</a>
      </div>
      <img class='x category__empty-image mxa' src='/images/bottle-straw.webp' alt='trashcan newsletter' />
    </div>
  </div>
</div>